{
  "name": "maintainiq-root",
  "version": "1.0.0",
  "description": "MaintainIQ root package delegator",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "install-frontend": "npm install --prefix frontend"
  }
}
